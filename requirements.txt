# Pinned for Docker layer caching — keep in sync with pyproject.toml
requests>=2.28
numpy>=1.24
pillow>=10.0
matplotlib>=3.7
librosa>=0.10
soundfile>=0.12
faster-whisper>=1.0
opencv-python-headless>=4.8
